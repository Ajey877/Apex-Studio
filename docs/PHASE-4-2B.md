# Phase 4.2B — Reproducibility Audit

This branch begins the reproducibility hardening pass. The dependency lockfile must be generated by npm from the repository's actual package.json; it must not be hand-authored.
