name: Release Apex Studio DAW (Windows & Multi-Platform)

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build Windows Standalone .EXE & Release
    runs-on: windows-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js Environment
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Project Dependencies
        run: npm install --legacy-peer-deps

      - name: Compile Optimized Web Bundle
        run: npm run build

      - name: Package Standalone Windows Executable (.exe)
        run: npx electron-builder --win --x64
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release & Upload Assets
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          name: "Apex Studio DAW ${{ github.ref_name }}"
          draft: false
          prerelease: false
          generate_release_notes: false
          body: "Official standalone Windows installer (.exe) for Apex Studio DAW. Unpack or install to run completely offline on Windows 10 & 11."
          files: |
            dist-electron/*.exe
            dist-electron/*.zip
            dist-electron/*.blockmap
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
